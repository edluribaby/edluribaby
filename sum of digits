#include<stdio.h>
void main()
{
    int a;
    printf("enter any num");
    scanf("%d",&a);
    int sum=0;A
    while(a>1)
    {
        sum=sum+a%10;
        a=a/10;
    }
    printf("the sum of digits is %d",sum);
}
